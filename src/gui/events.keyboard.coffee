# Eventful code comes here
# Program state should not be manipulated outside events files

keyDown = safeExport 'morpheus.gui: keyDown', undefined, (event) ->
  #switch event.which
  #  when 0
  return
      
